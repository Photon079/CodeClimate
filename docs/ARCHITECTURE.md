# Invoice Guard - Architecture Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER DEVICES                             │
│  Desktop Browser  │  Mobile Browser  │  Tablet Browser          │
└────────────┬────────────────┬────────────────┬───────────────────┘
             │                │                │
             └────────────────┴────────────────┘
                              │
                    ┌─────────▼──────────┐
                    │   FRONTEND APP     │
                    │  (Port 8000)       │
                    │  Vanilla JS        │
                    │  Tailwind CSS      │
                    └─────────┬──────────┘
                              │
                    ┌─────────▼──────────┐
                    │   localStorage     │
                    │  (Offline Data)    │
                    └─────────┬──────────┘
                              │
                    ┌─────────▼──────────┐
                    │  BACKEND API       │
                    │  (Port 3000)       │
                    │  Node.js/Express   │
                    │  (Optional)        │
                    └─────────┬──────────┘
                              │
                    ┌─────────▼──────────┐
                    │   DATABASE         │
                    │  MongoDB/Memory    │
                    │  (Future)          │
                    └────────────────────┘
```

## 📱 Frontend Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                      index.html                               │
│                   (Entry Point)                               │
└────────────────────────┬─────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
┌────────▼────────┐            ┌────────▼────────┐
│   CSS Layer     │            │   JS Layer      │
│                 │            │                 │
│  styles.css     │            │   app.js        │
│  - Light theme  │            │   (Controller)  │
│  - Dark theme   │            │                 │
│  - Responsive   │            └────────┬────────┘
│  - Animations   │                     │
└─────────────────┘         ┌───────────┴───────────┐
                            │                       │
                   ┌────────▼────────┐    ┌────────▼────────┐
                   │  Core Modules   │    │  Enhancement    │
                   │                 │    │  Modules        │
                   │  - invoice.js   │    │                 │
                   │  - storage.js   │    │  - search.js    │
                   │  - validator.js │    │  - theme.js     │
                   │  - formatter.js │    │  - export.js    │
                   │  - reminder.js  │    │  - demo.js      │
                   │  - ui.js        │    │                 │
                   │  - performance  │    │                 │
                   └─────────────────┘    └─────────────────┘
```

## 🔄 Data Flow

### Creating an Invoice

```
User Input
    │
    ▼
┌─────────────────┐
│  UI Layer       │  User fills form
│  (index.html)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Validation     │  Check inputs
│  (validator.js) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Business Logic │  Create invoice
│  (invoice.js)   │  Generate number
└────────┬────────┘  Calculate status
         │
         ▼
┌─────────────────┐
│  Storage        │  Save to localStorage
│  (storage.js)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  UI Update      │  Re-render dashboard
│  (ui.js)        │  Show toast
└─────────────────┘
         │
         ▼
    User sees result
```

### Search & Filter Flow

```
User types in search
         │
         ▼
┌─────────────────┐
│  Debounce       │  Wait 300ms
│  (performance)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Search Module  │  Filter invoices
│  (search.js)    │  Apply filters
└────────┬────────┘  Sort results
         │
         ▼
┌─────────────────┐
│  UI Update      │  Re-render table
│  (ui.js)        │  Show count
└─────────────────┘
         │
         ▼
    Filtered results
```

### Dark Mode Toggle

```
User clicks toggle
         │
         ▼
┌─────────────────┐
│  Theme Module   │  Toggle theme
│  (theme.js)     │  light ↔ dark
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  localStorage   │  Save preference
│  (storage.js)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  DOM Update     │  Add/remove 'dark'
│  (theme.js)     │  class
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  CSS Apply      │  Dark mode styles
│  (styles.css)   │  activate
└─────────────────┘
         │
         ▼
    Theme changed
```

## 🗄️ Backend Architecture (Optional)

```
┌──────────────────────────────────────────────────────────────┐
│                      server.js                                │
│                   (Express App)                               │
└────────────────────────┬─────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
┌────────▼────────┐            ┌────────▼────────┐
│   Middleware    │            │   Routes        │
│                 │            │                 │
│  - CORS         │            │  /api/invoices  │
│  - JSON Parser  │            │  /api/auth      │
│  - Error Handler│            │  /api/sync      │
└─────────────────┘            └────────┬────────┘
                                        │
                               ┌────────▼────────┐
                               │  Controllers    │
                               │                 │
                               │  - CRUD ops     │
                               │  - Validation   │
                               │  - Business     │
                               └────────┬────────┘
                                        │
                               ┌────────▼────────┐
                               │  Data Layer     │
                               │                 │
                               │  - In-Memory    │
                               │  - MongoDB      │
                               │  (Future)       │
                               └─────────────────┘
```

## 🧪 Testing Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                      Vitest Runner                            │
└────────────────────────┬─────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
┌────────▼────────┐            ┌────────▼────────┐
│  Unit Tests     │            │  Property Tests │
│                 │            │                 │
│  - Specific     │            │  - fast-check   │
│    examples     │            │  - 100+ runs    │
│  - Edge cases   │            │  - Random data  │
└─────────────────┘            └────────┬────────┘
                                        │
                               ┌────────▼────────┐
                               │  Test Modules   │
                               │                 │
                               │  - invoice      │
                               │  - storage      │
                               │  - validator    │
                               │  - formatter    │
                               │  - reminder     │
                               │  - ui           │
                               │  - app          │
                               └─────────────────┘
                                        │
                                        ▼
                               109 Tests Passing ✅
```

## 📦 Module Dependencies

```
app.js
  ├── storage.js
  ├── invoice.js
  │   ├── formatter.js
  │   └── performance.js (memoize)
  ├── validator.js
  ├── reminder.js
  │   └── formatter.js
  ├── ui.js
  │   ├── formatter.js
  │   └── performance.js (debounce)
  ├── search.js
  ├── theme.js
  ├── export.js
  │   └── formatter.js
  └── demo.js
```

## 🔐 Security Layers

```
┌──────────────────────────────────────────────────────────────┐
│                      User Input                               │
└────────────────────────┬─────────────────────────────────────┘
                         │
                ┌────────▼────────┐
                │  Input          │  Sanitize HTML
                │  Sanitization   │  Escape special chars
                └────────┬────────┘
                         │
                ┌────────▼────────┐
                │  Validation     │  Type checking
                │  Layer          │  Range validation
                └────────┬────────┘  Format validation
                         │
                ┌────────▼────────┐
                │  Business       │  Business rules
                │  Logic          │  Constraints
                └────────┬────────┘
                         │
                ┌────────▼────────┐
                │  Storage        │  localStorage
                │  Layer          │  (sandboxed)
                └─────────────────┘
```

## 🌐 Deployment Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                      Git Repository                           │
└────────────────────────┬─────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┬──────────────┐
         │                               │              │
┌────────▼────────┐            ┌────────▼────────┐    │
│   Netlify       │            │   Vercel        │    │
│                 │            │                 │    │
│  - Auto deploy  │            │  - Auto deploy  │    │
│  - CDN          │            │  - Edge network │    │
│  - SSL          │            │  - SSL          │    │
└─────────────────┘            └─────────────────┘    │
                                                       │
                                              ┌────────▼────────┐
                                              │  GitHub Pages   │
                                              │                 │
                                              │  - Static host  │
                                              │  - Free SSL     │
                                              └─────────────────┘
```

## 📊 Performance Optimization

```
┌──────────────────────────────────────────────────────────────┐
│                      User Action                              │
└────────────────────────┬─────────────────────────────────────┘
                         │
                ┌────────▼────────┐
                │  Debouncing     │  Reduce calls
                │  (300ms)        │
                └────────┬────────┘
                         │
                ┌────────▼────────┐
                │  Memoization    │  Cache results
                │  (LRU Cache)    │
                └────────┬────────┘
                         │
                ┌────────▼────────┐
                │  Batch DOM      │  Single repaint
                │  Updates        │
                └────────┬────────┘
                         │
                ┌────────▼────────┐
                │  Virtual        │  Render visible
                │  Scrolling      │  items only
                └─────────────────┘
                         │
                         ▼
                    Fast UI ⚡
```

## 🎨 Theme System

```
┌──────────────────────────────────────────────────────────────┐
│                      Theme Toggle                             │
└────────────────────────┬─────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
┌────────▼────────┐            ┌────────▼────────┐
│  Light Theme    │            │  Dark Theme     │
│                 │            │                 │
│  - #FFFFFF bg   │            │  - #111827 bg   │
│  - #111827 text │            │  - #F3F4F6 text │
│  - Bright       │            │  - Muted        │
└─────────────────┘            └─────────────────┘
         │                               │
         └───────────────┬───────────────┘
                         │
                ┌────────▼────────┐
                │  CSS Variables  │  Smooth
                │  Transitions    │  transition
                └─────────────────┘
```

## 🔄 State Management

```
┌──────────────────────────────────────────────────────────────┐
│                      Global State                             │
│                                                               │
│  - invoices: []                                              │
│  - currentFilters: {}                                        │
│  - enrichedInvoicesCache: null                               │
│  - lastInvoicesHash: null                                    │
└────────────────────────┬─────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
┌────────▼────────┐            ┌────────▼────────┐
│  localStorage   │            │  Memory Cache   │
│                 │            │                 │
│  - Persistent   │            │  - Fast access  │
│  - Survives     │            │  - Memoized     │
│    reload       │            │  - Temporary    │
└─────────────────┘            └─────────────────┘
```

## 📱 Responsive Breakpoints

```
320px          768px         1024px        1920px
  │              │              │             │
  ▼              ▼              ▼             ▼
Mobile        Tablet        Desktop      Large Desktop
  │              │              │             │
  ├─ Stack      ├─ 2 cols     ├─ 3 cols    ├─ Wide
  ├─ Full width ├─ Optimize   ├─ Full      ├─ Max 1536px
  └─ Touch      └─ Spacing    └─ Layout    └─ Optimize
```

## 🎯 Feature Modules

```
Core Features (MVP)
├── Invoice CRUD
├── Status Tracking
├── Late Fees
├── Reminders
└── Dashboard

Enhancement Features ⭐
├── Search & Filter
├── Dark Mode
├── Export/Import
├── Demo Data
└── Keyboard Shortcuts
```

---

**This architecture supports:**
- ✅ Scalability
- ✅ Maintainability
- ✅ Testability
- ✅ Performance
- ✅ Security
- ✅ Offline-first
- ✅ Mobile-responsive

{
  "name": "invoice-guard",
  "version": "1.0.0",
  "description": "Invoice tracking application for Indian freelancers",
  "type": "module",
  "scripts": {
    "dev": "http-server frontend -p 8000 -o",
    "start": "http-server frontend -p 8000 -o",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "fast-check": "^3.15.0",
    "jsdom": "^23.0.0",
    "http-server": "^14.1.1"
  }
}
